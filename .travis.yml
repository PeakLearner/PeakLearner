language: python

python:
  - 3.6
  - 3.7
  - 3.8

before_install:
  - python --version
  - pip install -U pip
  - pip install -U pytest
  - pip install requests
  - pip install pybbi
  - pip install pandas
  - pip install numpy
  - pip install git+https://github.com/deltarod/PeakError.git
  - pip install codecov

script:
  - pytest tests/

after_success:
  - codecov



